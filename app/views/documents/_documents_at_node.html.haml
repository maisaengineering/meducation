- documents_vault(category).each do |document|
  #demo3.docRow.getdoc{data: {'document'=> document.id, 'category'=> category.id}}
    .docMain{:onclick => ""}
      .docTitlePlus
        .docTitle
          %a{:href => "#"}= l(document.taken_on)
          .docSubTitle #{document.org_provider} #{document.expiration_date.nil? ? nil: append_dot("exp. " + l(document.expiration_date))} #{append_dot truncate(document.description, length:125)}
    .documentButtons
      -#.documentButton.dbView{:onclick => "window.open('#{bw_document_url(document)}');"}
      -#  = link_to 'View', '#'
      -#.documentButton.dbFullView{:onclick => "window.open('#{colored_document_url(document)}');"}
      -#  =link_to 'Full view', '#'
      -#.documentButton.dbEdit.document_edit
      -#  = link_to  'Edit', edit_document_path(document.profile_id ,document, user_tag: category.id)
    .clear
.clear